export default {
    GET_UPLOAD_IMAGE: (state) => {
        return state.uploadImage;
    } ,
    GET_LIST_IMAGE: (state) => {
        return state.listImage;
    },
    GET_OLD_IMAGE: (state) => {
        return state.oldImage;
    }
}
